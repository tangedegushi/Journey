<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <ImageView android:id="@+id/flash_item_img"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />
    <TextView android:id="@+id/flash_item_btn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="150dp"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:gravity="center"
        android:text="@string/str_begin"
        android:textSize="18sp"
        android:textColor="#ffffff"
        android:drawableRight="@mipmap/guide_next"
        android:drawableEnd="@mipmap/guide_next"
        android:paddingRight="30dp"
        android:paddingLeft="25dp"
        android:background="@mipmap/guide_btn"
        android:visibility="gone"/>

</RelativeLayout>
